---

# This role is applied to a single host during post provisioning

- name: "Validate the kvm prerequisites"
  ansible.builtin.include_tasks: "ensure_deploy_image_instance.yml"

- name: "Ensure libvirt realm integration"
  ansible.builtin.include_tasks: "ensure_image_configuration.yml"

- name: "Configure libvirt connection"
  ansible.builtin.include_tasks: "ensure_anonymize_instance.yml"

- name: "Configure libvirt connection"
  ansible.builtin.include_tasks: "ensure_convert2template.yml"

- name: "Configure libvirt connection"
  ansible.builtin.include_tasks: "ensure_register_image_2_satellite.yml"
...
